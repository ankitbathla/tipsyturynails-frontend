import React from "react";

import { Loader, LoaderContainer } from "./style";

const Spinner = () => {
    return (
        <LoaderContainer>
            <Loader />
        </LoaderContainer>
    );
};
export default Spinner;
